using UnityEngine;
using System.Collections;

public class GameMaster : MonoBehaviour {
	//public float newsTimer;
	//public float newsTimerLimit = 10;

	// Use this for initialization
	void Start () {
		//newsTimer = newsTimerLimit;
	}
	
	// Update is called once per frame
	void Update () {
		//newsTimerLimit -= Time.deltaTime;

	}
}
